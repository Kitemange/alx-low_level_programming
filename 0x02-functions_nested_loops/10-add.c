#include "main.h"
/**
 * add - a function that adds two integers and returns the result
 * @c: first number input
 * @d: second number input
 * Return: one + two
 */
int add(int c, int d)
{
	return (c + d);
}
