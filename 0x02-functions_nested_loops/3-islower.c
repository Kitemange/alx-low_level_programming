#include<stdio.h>
#include "main.h"

/**
 * _islower - lowercase_letters.
 *
 * Return: 1,0.
 * 
 * c - input sample
 */

int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
    /* code */
return (1);
}
else
{
return (0);
}
}
