#include<stdio.h>
#include "main.h"

/**
 * @brief 
 *
 * 
 */

int _isalpha(int c)
{
if (/* condition */c >= 'a' && c <= 'z' && c >= 'A' && c <= 'Z')
{
    /* code */
return (1);
}
else
{
return (0);
}
}