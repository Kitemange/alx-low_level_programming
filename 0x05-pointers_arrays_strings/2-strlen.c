#include "main.h"
#include <stdio.h>
#include<string.h>

/**
 * _strlen - returns lenght of string
 * @s: input parameter
 *
 * Description: uses pointers
 * Return: Alawy (0)
 */
int _strlen(char *s)
{
int l = 0;

while (/* condition */s[l] != '\0')
{
	/* code */
	continue;
}

return (l);
}
