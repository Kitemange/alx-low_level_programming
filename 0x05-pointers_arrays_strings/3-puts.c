#include "main.h"
#include<stdio.h>

/**
 * _puts - put string
 * @str: first parameter
 *
 * Description: prints a string
 * Return: Alwalys (0)
 */

void _puts(char *str)
{
	while (*str != '\0')
	{
		int puts(const char *str);
		str++;
	}
}
