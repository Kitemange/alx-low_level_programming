#include <stdio.h>

/**
 * main - alphabet in lowercase, followed by a new line.
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
double b, base;
b = 0;
b++;
/* finding value of log10 */
base = log10(b);
printf("log10(%ld) = %ld\n", b, base);

return (0);
}
